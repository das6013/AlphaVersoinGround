using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//[RequireComponent(typeof(„N„p„x„r„p„~„y„‘ „ƒ„‚„y„„„„p))] „R„„t„p „ƒ„r„€„z „ƒ„{„‚„y„„„ „x„p„„y„‡„p„u„„„u 
[RequireComponent(typeof(Plant))]
public class Tree : MonoBehaviour
{
    private bool choppable;
    // Start is called before the first frame update
    void Awake()
    {
        choppable = true;
    }
    //private void OnCollisionEnter(Collision collision)
    //{
    //    if (collision.collider.GetComponent < "„N„p„x„r„p„~„y„u „r„p„Š„u„s„€ „€„q„Œ„u„{„„„p(„„„€„„p„‚„p)" > () != null)
    //    {
    //        //If tree cut to 2 pieces,then  cast chopable one=chopavle one .New chopable2
    //        if (chopable2 >= (SGameObject)) Destroy(Plant);
    //    }

    //}
    
    
        
    

  
}
